{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix the-stack",
    "build": "npm run build --prefix the-stack",
    "start": "npm run start --prefix the-stack",
    "lint": "npm run lint --prefix the-stack"
  }
}
